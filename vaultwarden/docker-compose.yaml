services:
  vaultwarden:
    image: vaultwarden/server
    hostname: vaultwarden
    volumes:
      - vaultwarden:/data
    env_file: .env
    environment:
      DOMAIN: https://clic.epfl.ch/armoire
    
    networks:
      caddy:

networks:
  caddy:
    external: true
volumes:
  vaultwarden: