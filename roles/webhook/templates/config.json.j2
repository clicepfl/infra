{
  "secret": "{{ service.github.secret }}",
  "github_access_token": "{{ service.github.access_token }}",
  "default": {
    "start_command": "ansible-pull -U {{ service.repository }} -e @{{ service.secrets_file }} deploy.yaml --extra-vars SERVICE=$SERVICE"
  },
  "services": {
    {{ webhook_services }}
  }
}
